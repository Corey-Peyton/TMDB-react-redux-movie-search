import { render, screen } from '@testing-library/react';
import MovieBlock from './index';

describe('components/MovieBlock', () => {
	test('truth is truthy', () => {
		expect(true).toBe(true);
	});
})
